#all of this are done in 0(1)
stack = []
stack.append() #push the the end of the stack
stack.pop() #pop from the start

stack[-1] #is the top element

#queue in python
from collections import deque #this is how we implement queue in python
q = deque()
q.append() #adding to a queue is enqueue
q.popleft() #removing form a queue is dequeue

q[0] #is the front of the queue
q[-1] #is the back of the queue

